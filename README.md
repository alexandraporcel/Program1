
# Program1
Here u can see my classes with profehugonex
Clases de Rust con profehugonex.

## Contenido

| Clase | Tema |
|-------|------|
| [clase1](clase1) | Variables con `let` y `let mut` |
| [clase2](clase2) | Recuerdos del uso del `if...else`, `loop`, `while`, `for` y `funciones`  |

## Notas
- Usamos la terminal y VS Code para compilar y verificar el código.
- We used the terminal and VS Code to compile and verify the code.
- El comando `cargo run` compila y ejecuta el programa.
