In this class, we looked at how to use the `let` or `let mut` command, and obviously, we used the terminal and VS Code to verify it
En esta clase, nosotros vimos como usar el let o let mut y obviamente, usamos la terminal ya sea por cmd o por VS Code to para verficarlo, tambien en el proceso de intalación el como era
